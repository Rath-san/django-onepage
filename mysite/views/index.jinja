<!DOCTYPE html>
<html lang="en">
{% include './components/head.jinja' %}

<body>
    {% include './components/nav.jinja' %}
    {# {% include './components/macro.jinja' %} #}
    {# {% include './components/bundle-popup.jinja' %} #}

    {% macro picture(
		media_url,
		filename,
		media_files={},
		file_sizes=[360, 480, 640, 960],
		formats=['webp','jpg'],
		media_sizes='',
		media='',
		alt='',
		img_class='',
		img_style='',
		picture_class='',
		width='',
		height='',
        loading='',
        data_src=False
	) %}
		<picture class="{{ picture_class }}">
			{% for media in media_files %}
				{% set m = media_files[media] %}
				{% for format in formats %}
					<source
                        {% if data_src %}
                            data-srcset="
                                {% for size in m['file_sizes'] %}
                                    {{ media_url }}/{{ m['filename'] }}-{{ size }}.{{ format }} {{size}}w,
                                {% endfor %}"
                        {% else %}
                            srcset="
                                {% for size in m['file_sizes'] %}
                                    {{ media_url }}/{{ m['filename'] }}-{{ size }}.{{ format }} {{size}}w,
                                {% endfor %}"
                        {% endif %}
						type="image/{{ format | replace('jpg', 'jpeg') }}"
						sizes="{{ m['media_sizes'] | default(media_sizes) }}"
						media="{{ media }}"
					>
				{% endfor %}
			{% endfor %}
			{% for format in formats %}
				<source
                    {% if data_src %}
                    data-srcset="
						{% for size in file_sizes %}
							{{ media_url }}/{{ filename }}-{{ size }}.{{ format }} {{size}}w,
						{% endfor %}"
                    {% else %}
                    srcset="
						{% for size in file_sizes %}
							{{ media_url }}/{{ filename }}-{{ size }}.{{ format }} {{size}}w,
						{% endfor %}"
                    {% endif %}
					type="image/{{ format | replace('jpg', 'jpeg') }}"
					sizes="{{ media_sizes }}"
					media="{{ media }}">
			{% endfor %}
			<img
                {% if data_src %}
                    data-src="{{ media_url }}/{{ filename }}-{{ file_sizes[0] }}.{{ formats[-1] }}?{{file_version}}"
                {% else %}
                    src="{{ media_url }}/{{ filename }}-{{ file_sizes[0] }}.{{ formats[-1] }}?{{file_version}}"
                {% endif %}
				alt="{{alt}}"
				class="{{img_class}}"
				style="{{img_style}}"
				width="{{width}}"
				height="{{height}}"
                loading="{{loading}}"
                >

		</picture>
	{% endmacro %}

    {% set srcsetSizesMobile = [
        640,
        960
    ] %}

    {% set srcsetSizesDesktop = [
        1280,
        1920,
        2560
     ] %}

     {% set srcsetSizes = srcsetSizesMobile + srcsetSizesDesktop %}

    {% set template_id = '1393' %}
    {% set file_version = 3 %}
    
    {% set media_prefix = 'mMockup' %}
    {% set media_url = 'https://s3.motionvfx.com/mvfxpublic/products/templates/%s/media' % (template_id) %}
    {% set placeholder_url = 'https://via.placeholder.com' %}

    {% set data = {
        'header': {
            'subtitle': 'Animated mockups collection for Final Cut Pro'
        },
        'trailer': {
            'title': 'Watch trailer',
            'text': 'See how big a bang you’ll make… whatever you need to present.'
        },
        'tutorial': {
            'title': 'Watch tutorial',
            'text': 'Understand the creative power mMockup puts in your hands.'
        },
        'promo-phone': {
            'title': 'Make any content stand <br>out with <span class="big">energy and style</span>',
            'text': 'Getting your point across - that’s what it’s all about. Get noticed, leave your mark!',
        },
        'promo-laptop': {
            'title': 'Don’t just show it. <span class="big">Show it off!</span>',
            'text1': 'Present apps, interfaces or functionalities with lightness and style!',
            'text2': 'Business owner? Make your offers and promos close deals!',
            'text3': 'Data or diagrams? <br>Get your viewers unmatched clarity!',
        },
        'promo-carousel-flat': {
            'titles': [
                'Stunning galleries',
                'Engaging web presentations',
                'Your product on display',
            ],
            'text': 'Fill in your timeline with entertaining movement choosing from a wide variety of ready-made presets',
        },
        'prevs': {
            'title': 'Animated mockups collection for Final Cut Pro',
            'w': '',
            'e': '',
        },
        'prevs-after': {
            'title': 'Present anything!',
        },
        'footer': {
            'title': 'Show it like it’s </br><span class="big">never been shown before!</span>',
            'text': 'Make anything you present leave a lasting mark on your audience.'
        }
    } %}

    {% set trailer_id = 'o5YUZ38JEjE' %}
    {% set tutorial_id = '8lZ_cwQ-XHU' %}

    <main>
        <section class="header">
            <div class="image__background">
                {{picture(
                    media_url=media_url,
                    filename= media_prefix + '_screensa' ,
                    file_sizes=srcsetSizes,
                    formats=['webp','png'],
                    width=2560,
                    height=1440,
                    img_class='screens ',
                    alt="Header image",
                )}}
                {{picture(
                    media_url=media_url,
                    filename= media_prefix + '_isometrica' ,
                    file_sizes=srcsetSizes,
                    formats=['webp', 'png'],
                    width=2560,
                    height=1420,
                    img_class='isometric',
                )}}
            </div>
            <div class="container-fluid">
                <div class="section__content">
                    <div class="header__logo">
                        <img src="{{media_url}}/{{media_prefix}}_logo.png?{{file_version}}"
                            alt=""
                            srcset=""
                            width="500"
                            height="269">
                        <h2 hidden class="">mMockup</h2>
                    </div>

                    <h1 class="section__text">
                        {% autoescape off %} {{product.head_info}} {% endautoescape %}
                    </h1>

                    <img class="fcp"
                        src="{{media_url}}/fcp-152.png?{{file_version}}"
                        alt="Final Cup Pro icon"
                        width="76"
                        height="76"
                    >
                </div>
            </div>
        </section>

        <script>
            const onHeaderImagesLoaded = (images, type= 'primary') => {
                const loadedImages = images.map(img => false);
                images.forEach((img, idx) => {
                    loadedImages[idx] = img.complete;

                    if (loadedImages.every(val => val)) {
                        header.classList.add(`loaded-${type}`)
                    }
                    img.onload = () => {
                        loadedImages[idx] = true
                        if (loadedImages.every(val => val)) {
                            header.classList.add(`loaded-${type}`)
                        }
                    }
                })
            }

            const header = document.querySelector('section.header')
            const items1 = [
                header.querySelector('.header__logo img'),
                header.querySelector('.fcp')
            ]

            const items2 = Array.from(header.querySelectorAll('.image__background img'));

            onHeaderImagesLoaded(items1);
            onHeaderImagesLoaded(items2, 'secondary');
        </script>

        <div class="scroll-prompt" style="--h-index: 2; opacity: 0;">
            <a class="scroll-to-btn" href="#" onclick="scrollToAnchor('trailer');" tabindex="-1"></a>
        </div>

        <a class="nav" name="trailer"></a>

        <section class="video trailer">
            <div class="container">
                <div class="row">
                    <div class="col">
                        <div class="section__content">
                            <div class="section__head">
                                <div class="section__title">
                                    {% autoescape off %} {{data.trailer.title}} {% endautoescape %}
                                </div>
                                <p class="section__text">
                                    {% autoescape off %} {{data.trailer.text}} {% endautoescape %}
                                </p>
                            </div>
                        </div>
                        
                        <div class="yt embed-responsive embed-responsive-16by9">
                            <div id="videoTrailer" data-id="{{trailer_id}}" style="background-color: #000"></div>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <a class="nav" name="tutorial"></a>

        {{picture(
            media_url=media_url,
            filename= media_prefix + '_imac' ,
            file_sizes=[385, 770],
            formats=['webp','png'],
            width=770,
            height=690,
            data_src=True,
            alt="",
            picture_class="deco deco--video",
            img_class="lazy-show"
        )}}

        <section class="video tutorial">
            <div class="container text-center">
                <div class="row">
                    <div class="col-12">
                        <div class="section__content">
                            <div class="section__head">
                                <p class="section__subtitle " style="--s-index:0;">
                                    {{data.tutorial.subtitle}}
                                </p>
                                <div class="section__title ">
                                    {% autoescape off %} {{data.tutorial.title}} {% endautoescape %}
                                </div>
                                <p class="section__text " style="--s-index:2;">
                                    {% autoescape off %} {{data.tutorial.text}} {% endautoescape %}
                                </p>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-12">
                        <div class="yt embed-responsive embed-responsive-16by9">
                            <div id="videoTutorial" data-id="{{tutorial_id}}" style="background-color: #000"></div>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <section class="promo promo-phone">
            <div class="container">
                <div class="row">
                    <div class="col-lg-6">
                        <div class="phone-comp">
                            {{picture(
                                picture_class="phone-comp__image",
                                media_url=media_url,
                                filename= media_prefix + '_mobile_1' ,
                                file_sizes=[400],
                                formats=['webp', 'png'],
                                width=400,
                                height=630,
                                data_src=True,
                                img_class='lazy-show',
                                alt=""
                            )}}
                            {{picture(
                                picture_class="phone-comp__image",
                                media_url=media_url,
                                filename= media_prefix + '_mobileman' ,
                                file_sizes=[400],
                                formats=['webp', 'png'],
                                width=400,
                                height=605,
                                data_src=True,
                                img_class='lazy-show',
                                alt=""
                            )}}
                        </div>
                    </div>
                    <div class="col-lg-6">
                        <div class="section__content">
                            <div class="section__head">
                                <div class="section__title">{{data['promo-phone'].title|safe}}</div>
                                <div class="section__text">{{data['promo-phone'].text}}</div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <section class="promo promo-laptop">
            <div class="container">
                <div class="row">
                    <div class="col-lg-6">
                        <div class="promo-laptop-comp">
                            {{picture(
                                picture_class="promo-laptop__image",
                                media_url=media_url,
                                filename= media_prefix + '_macbook_1' ,
                                file_sizes=[500, 1000, 1500, 2000],
                                formats=['webp', 'png'],
                                width=2000,
                                height=1587,
                                data_src=True,
                                img_class='lazy-show mac',
                                alt=""
                            )}}
                            {{picture(
                                picture_class="promo-laptop__image",
                                media_url=media_url,
                                filename= media_prefix + '_macbook_ziom_1' ,
                                file_sizes=[500, 1000, 1500],
                                formats=['webp', 'png'],
                                width=1500,
                                height=1251,
                                data_src=True,
                                img_class='lazy-show man',
                                alt=""
                            )}}
                            <div class="bauble bauble-1"></div>
                            <div class="bauble bauble-2"></div>
                        </div>
                    </div>
                    <div class="col-lg-6">
                        <div class="section__content">
                            <div class="section__head">
                                <div class="section__title">{{data['promo-laptop'].title|safe}}</div>
                                <div class="section__text">{{data['promo-laptop'].text1}}</div>
                                <div class="section__text">{{data['promo-laptop'].text2}}</div>
                                <div class="section__text">{{data['promo-laptop'].text3|safe}}</div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <section class="promo promo-carousel-flat">
            <div class="container">
                <div class="row">
                    <div class="col">
                        <div class="section__content">
                            <div class="section__head">
                                <div id="carouselFlatText" class="carousel slide carousel-fade">
                                    <div class="carousel-inner">
                                        {% for title in data['promo-carousel-flat'].titles %}
                                            <div class="carousel-item {% if loop.index == 1 %} active {% endif %}">
                                                <span class="section__title">{{title}}</span>
                                            </div>
                                        {% endfor %}
                                    </div>
                                </div>
                                <div class="section__text">{{data['promo-carousel-flat'].text}}</div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            {% set slides = [
                {
                    'src': '_slidera_2',
                    'alt': ''
                },
                {
                    'src': '_slidera_1',
                    'alt': ''
                },
                {
                    'src': '_slidera_3',
                    'alt': ''
                }
            ] %}
            <ol id="carouselFlatIndicators" class="carousel-indicators">
                {% for indicator in slides %}
                    <li class="{% if loop.index == 1 %} active {% endif %}"></li>
                {% endfor %}
            </ol>
            <div id="carouselFlat" class="carousel slide slide-new">
                <div class="carousel-inner">
                    {% for img in slides %}
                        <div class="carousel-item {% if loop.index == 1 %} active {% endif %}">
                            {{picture(
                                media_url=media_url,
                                filename= media_prefix + img.src ,
                                file_sizes=srcsetSizes,
                                formats=['jpg'],
                                width=2560,
                                height=960,
                                data_src=True,
                                img_class='d-block w-100 h-auto lazy-show',
                                alt=""
                            )}}
                        </div>
                    {% endfor %}
                </div>
                <a class="carousel-control-prev" href="#carouselFlat" role="button" data-slide="prev">
                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    <span class="sr-only">Previous</span>
                </a>
                <a class="carousel-control-next" href="#carouselFlat" role="button" data-slide="next">
                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    <span class="sr-only">Next</span>
                </a>
            </div>
        </section>

        <section class="prevs center">
            {% set productPageTemplateId = template_id %} 
            {% set collapseAfter = 8 %}
            {% set namePrefix = '' %}
            {% set categories = [
                    { 'key':'effect', 'label': 'Effects', 'count': 4},
                    { 'key':'basic', 'label': 'Basic', 'count': 25},
                    { 'key':'box', 'label': 'Box', 'count': 6},
                    { 'key':'computer', 'label': 'Computer', 'count': 15},
                    { 'key':'custom', 'label': 'Custom', 'count': 9},
                    { 'key':'isometric', 'label': 'Isometric', 'count': 13},
                    { 'key':'laptop', 'label': 'Laptop', 'count': 11},
                    { 'key':'smartphone', 'label': 'Smartphone', 'count': 14},
                    { 'key':'typography', 'label': 'Typography', 'count': 5},
                    { 'key':'background', 'label': 'Backgrounds', 'count': 12},
                ]
            %}
            
            <div class="container">
                <div class="row">
                    <div class="col-12 text-center">
                        <div class="section__content">
                            <div class="section__head">
                                <p class="section__display">
                                    114
                                </p>
                                <h2 class="section__title">
                                    fully animated, customizable elements
                                </h2>
                                <p class="section__subtitle">
                                    hover or tap to preview animations
                                </p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div id="product-prev-grid" class="product-prev-grid container">
    			{% for category in categories %}
					{% set key = category.key %}
					{% if categories|length > 1 %}
                        <p class="h3 prevs__title text-center">{{ category.label }}</p>
                    {% endif %}
					<div class=" row {{ category.rowClass | default('') }} {% if category.count == 5 %}row-5{% endif %}" >
						{% for i in range(category.count) %}
							{% set num = '%02d' % loop.index %}
							{% set filename = 'https://s3.motionvfx.com/mvfxpublic/products/templates/' + productPageTemplateId + '/media/' + namePrefix + key +  '_' + num %}
							<div class="
                                prev-card
                                product-prev-col
                                col-6
                                {# {% if key == 'subtitles' or key == 'typography' %}col-lg-4{% else %}col-lg-3{% endif %} #}
                                col-lg-3
                                {% if loop.index > collapseAfter %}collapse prev-collapse-{{ key }}{% endif %}
                            " id="prev-collapse-{{ key }}-{{ loop.index }}">
								<div class="product-prev-cell" data-mp4-src="{{ filename }}.mp4?{{file_version}}" style="--s-index: {{loop.index % 5}}" >
									<img loading="lazy"
                                        src="{{ filename }}-640.jpg?{{file_version}}"
                                        {# src="{{placeholder_url}}/400x225/eeaaff" #}
                                        alt="" width="400" height="225" />
									<div class="loading-spinner"></div>
								</div>
							</div>
						{% endfor %}
						{% if category.count > collapseAfter %}
							<div class="col-btn-collapse">
								<button class="btn-collapse" type="button" data-target=".prev-collapse-{{ key }}" aria-expanded="false" aria-controls="{% for j in range(collapseAfter, category.count) %} prev-collapse-{{ key }}-{{ j }} {% endfor %}">
									<span><span class="label-collapsed">Show more</span><span class="label-expanded">Collapse</span> ({{ category.count - collapseAfter }})</span>
									<svg version="1.1" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" x="0px" y="0px"
											viewBox="0 0 36.6 61.5" style="enable-background:new 0 0 36.6 61.5;" xml:space="preserve" class="btn-collapse-arrow" height="20">
										<style type="text/css">
											.st0{fill:none;stroke:currentColor;stroke-width:8;stroke-miterlimit:10;}
										</style>
										<polyline class="st0" points="2.8,58.6 30.9,30.5 3.2,2.8 "/>
									</svg>
								</button>
							</div>
						{% endif %}
					</div>
				{% endfor %}
            </div>
        </section>

        <section class="prevs-after">

            {# <div class="image__background"> #}
                <div class="section__content">
                    <div class="section__head">
                        <div class="section__title">
                            {{data['prevs-after'].title|safe}}
                        </div>
                    </div>
                </div>
                <div class="container">
                    <div class="comp">
                        {% for m in [
                            {
                                'src': '',
                                'alt': '',
                                'index': '1'
                            },
                            {
                                'src': '',
                                'alt': '',
                                'index': '2'
                            },
                            {
                                'src': '',
                                'alt': '',
                                'index': '3'
                            },
                            {
                                'src': '',
                                'alt': '',
                                'index': '5'
                            },
                            {
                                'src': '',
                                'alt': '',
                                'index': '4'
                            }
                        ] %}
                        {{picture(
                            media_url=media_url,
                            filename= media_prefix + '_s' + loop.index|string,
                            file_sizes=[250, 500],
                            formats=['jpg'],
                            width=400,
                            height=650,
                            img_class='comp__item comp__item--' + loop.index|string,
                            img_style="--s-index: " + m.index|string,
                            alt="",
                            loading="lazy"
                        )}}
                        {% endfor %}
                    </div>
                </div>
                {# <picture>
                    
                    <source media="(max-width: 992px)" srcset="
                        {% for size in srcsetSizesMobile %}
                            {{mediaUrl}}/hands_bg.jpg {{size}}w,
                        {% endfor %}
                    ">
                    <img
                        loading="lazy"
                        src="{{mediaUrl}}/hands_bg.jpg"
                        alt="Editor recording voiceover for video production"
                        srcset="
                            {% for size in srcsetSizesDesktop %}
                                {{mediaUrl}}/hands_bg.jpg {{size}}w,
                            {% endfor %}
                        "
                        sizes="100vw">
                </picture> #}
            {# </div> #}
        </section>

        <a class="nav" href="#" name="specs" rel="noopener noreferrer"></a>

        <div class="ts-mi">
            <section class="tech-spec">
                <div class="container">
                    <div class="row">
                        <div class="col-12">
                            <div class="tech-spec-info">
                                <div class="section__text__content">
                                    <div class="section__head">
                                        <div class="section__title tech-spec__title section__title">
                                            Technical specifications
                                        </div>
                                    </div>
                                    <div class="ttable tech-spec__table">
                                        <div class="tcell tlabel">Supported software</div>
                                        <div class="tcell tdata">
                                            <h3 class="p">
                                                {% autoescape off %}
                                                    {{ product.productdetails.minimal_requirements.replace('FCP 10', 'FCP 10').replace('Motion 5', 'Motion 5').replace(', ', ',<br>') }}
                                                {% endautoescape %}
                                            </h3>
                                        </div>
                                        <div class="tcell tlabel">Includes</div>
                                        <div class="tcell tdata">
                                            <h3 class="p">
                                                <span>114 titles</span>
                                            </h3>
                                        </div>
                                        <div class="tcell tlabel">Size</div>
                                        <div class="tcell tdata">
                                            {{ "%.0f MB"|format(product.productdetails.source_size / 1000) }}
                                        </div>
                                        {% for kind in ['trailer', 'tutorial'] %}
                                            {% with musics=product.productdetails.music.select_related('music_portal').all().filter(used_in_trailer=kind=='trailer') %}
                                                {% if musics|length %}
                                                    <div class="tcell tlabel">Music used in {{ kind }}</div>
                                                    <div class="tcell tdata">
                                                        {% include 'product/music_info.jinja2' %}
                                                    </div>
                                                {% endif %}
                                            {% endwith %}
                                        {% endfor %}
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
            </section>

            <section class="minstaller__section">
                <div class="container">
                    <div class="row">
                        <div class="col">
                            <div class="minstaller">
                                <div class="minstaller__title">mInstaller</div>
                                <p class="minstaller__description">
                                    After purchasing, the product is <b>instantly available </b>for download via <a href="https://www.motionvfx.com/page/minstaller.html" target="_blank" rel="noopener noreferrer">mInstaller</a>.
                                </p>
                            </div>
                        </div>
                    </div>
                </div>
            </section>
        </div>

        <footer class="footer">
            <div class="image__background " style="--s-index:0">
                {{picture(
                    media_url=media_url,
                    filename= media_prefix + '_footer_3' ,
                    file_sizes=srcsetSizes,
                    media_files={
                        '(max-width: 960px)': {
                            'filename': media_prefix + '_footer_mobile',
                            'formats':['jpg'],
                            'file_sizes': [640, 960],
                        }
                    },
                    formats=['jpg'],
                    width=2560,
                    height=1100,
                    data_src=True,
                    img_class='lazy-show',
                    alt="Footer"
                )}}
            </div>
            <div class="container">
                <div class="row">
                    <div class="col-12">
                        <div class="section__text__content">
                            <div class="section__head">
                                <div class="footer__title section__title">
                                    {{data.footer.title|safe}}
                                </div>
                                <p class="footer__text section__text ">
                                    {{ data.footer.text }}
                                </p>
                            </div>
                            <div class="button-buy">
                                {% with discount_text="INTRO", version=2 %}
                                    {% include "product/buy_button.jinja2" %}
                                {% endwith %}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </footer>
    </main>

    {# {% include './components/popup-left.jinja' %} #}
    {% include './components/submenu.jinja' %}

    <script src="dist/scripts.js"></script>
    <script src="scripts/vendor/menu.js" defer></script>
    <script src="scripts/vendor/scroll_to_anchor.js" defer></script>
</body>

</html>
